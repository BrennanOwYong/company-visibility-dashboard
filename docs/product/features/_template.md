# Feature: <feature-id>

Parent PRD: `docs/product/PRD.md#<anchor>`

## Purpose

Why this feature exists in the user's words.

## User outcome

What becomes easier, possible, safer, or clearer for the user.

## Scope and non-goals

What the user expects this feature to cover and explicitly not cover.

## Positive flow

Walk through a successful attempt from the user's point of view, including what they begin with,
what they choose or provide, and what they see after each meaningful action.

Use numbered user actions. Name the visible result after each action. The browser tester uses these
steps as the successful browser journey.

## Negative and recovery flow

Describe invalid choices, unavailable states, interruptions, permissions, empty results, and other
expected failures. State what the user sees, what must not happen, and how they recover.

Use numbered user actions for each negative or recovery journey. The browser tester uses these
steps to test rejection, interruption, empty, permission, and recovery behavior.

## User inputs and visible outcomes

Describe what a person supplies or chooses and what appears, changes, or is delivered as a result.
Use product language, not technical schemas.

## Done outcomes

Use stable IDs (`DC-01`). State observable product outcomes in Given/When/Then form without
implementation detail.

## Subjective user-test questions

List only what requires human judgment: clarity, confidence, perceived effort, flow, or feel. Use
`(none)` for backend-only features.

## Applicable principles

Link confirmed entries in `docs/product/principles.md`, or state `(none identified)`.

## Open questions

Unresolved product questions only.

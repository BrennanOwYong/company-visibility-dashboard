# Setup

Normal local work uses the deterministic provider simulator. A live adapter needs registered redirect
URLs, least-access test accounts, vault entries, rate-limit guidance, and a documented cleanup path.
Missing live setup creates human intervention; it does not turn a simulator pass into a live pass.


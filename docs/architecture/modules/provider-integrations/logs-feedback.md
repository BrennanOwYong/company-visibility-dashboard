# Logs and feedback

Emit authorization state, discovered capability counts, provider request outcome, duration, rate
limit, refresh freshness, normalization outcome, and revocation result. Use provider key and safe
hashed account identity. Never log redirect query values, credentials, response bodies, or metric
values.


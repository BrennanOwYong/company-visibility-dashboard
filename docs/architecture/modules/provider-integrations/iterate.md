# Iterate

Add or change one provider only behind the adapter contract. Update its capability mapping, safe
error mapping, simulator cases, grants, documentation, and affected page fixtures together. Do not
generalize two provider concepts into one canonical metric until units and business meaning match.


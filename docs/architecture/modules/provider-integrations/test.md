# Test

Every adapter must pass shared authorization, capability, normalization, pagination, time-window,
rate-limit, cancellation, expiration, revocation, empty, partial, malformed response, timeout, and
outage tests. Add a sandbox or approved live smoke test where the provider offers it. Verify that
test evidence contains no token, authorization code, cookie, or raw provider body.


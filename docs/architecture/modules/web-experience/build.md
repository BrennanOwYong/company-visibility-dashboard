# Build

Build one versioned browser asset set from the same contract version as the server. Theme values,
approved icons, status patterns, and generated-page components are centralized. The generated-page
renderer cannot accept script or component source from a page definition.


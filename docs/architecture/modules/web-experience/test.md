# Test

Use component tests for every status and browser journeys from each product feature. Run keyboard,
focus, accessible-name, contrast, enlarged-text, narrow-layout, reconnect, refresh, empty, partial,
expired, denied, and loading-failure cases. The web-control tester uses `agent-browser` and records
screenshots plus correlated frontend/backend event evidence for every mapped criterion.


# Module: web-experience

## Responsibility

Own the soft pastel orange browser shell, accessible side menu, connection and Memory views, page
chat, generated-page renderer, durable progress stream client, and visible recovery states.

## Repository paths

Planned: `apps/web/` and shared browser-facing types in `packages/contracts/`.

## Public interfaces

Uses `../../contracts/http-api.md`, `../../contracts/page-definition.md`, and
`../../contracts/events.md`. It owns no provider or credential interface.

## Current feature ownership

Primary presentation for all five active features.

## Decisions

See `decisions.md`.

## Current-PRD lessons

See `current-prd-lessons.md`.


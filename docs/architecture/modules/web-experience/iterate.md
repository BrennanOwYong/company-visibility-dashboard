# Iterate

Add user-visible states to the shared status model before feature-specific rendering. A new generated
widget requires a page-definition contract update, validator rule, fixture states, accessibility
behavior, and browser tests. Do not put provider-specific interpretation in the browser.


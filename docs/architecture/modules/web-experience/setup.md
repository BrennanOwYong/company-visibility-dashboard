# Setup

Local setup uses the server API, deterministic identity, provider simulator, Memory fixtures, page
definitions for every data state, and an event-stream fault switch. No real provider credential is
required for normal browser tests. Live smoke tests use approved external setup.


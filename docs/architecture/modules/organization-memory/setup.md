# Setup

Local setup needs the shared secret scanner, a deterministic Memory-assistant fixture, safe account
and capability fixtures, and conversations for new, accepted, conflicting, interrupted, sensitive,
and unauthorized states. A live model is optional for local development and required only in an
approved external environment.


# Module: organization-memory

## Responsibility

Own the Memory conversation, secret-safe message persistence, focused clarification, contradiction
detection, typed summary proposals, acceptance, and version selection for later page builds.

## Repository paths

Planned: `packages/organization-memory/` with browser presentation in `apps/web/`.

## Public interfaces

Memory routes in `../../contracts/http-api.md`, Memory records in `../../contracts/data-model.md`, and
redaction in `../../contracts/security-boundaries.md`.

## Current feature ownership

Primary owner for `company-memory`; supplies the accepted summary version to `page-system`.

## Decisions

See `decisions.md`.

## Current-PRD lessons

See `current-prd-lessons.md`.


# Iterate

A summary schema change needs version reading, migration or compatibility behavior, prompt contract,
validation, browser presentation, and page-build context tests. Do not store an assistant response as
accepted meaning without explicit user acceptance. Do not couple a Memory concept directly to one
provider's vocabulary.


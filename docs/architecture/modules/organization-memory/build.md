# Build

Keep conversation append, redaction, summary proposal, validation, conflict analysis, and acceptance
as separate use cases. The typed summary schema is versioned. Model-client code depends on a provider
interface and cannot resolve credentials or call creator services.


# Test

Test correlation from browser action through API, job, provider or model, state change, and final
render. Test safe-attribute allowlists, bounded metric labels, secret redaction, export failure,
duplicate events, replay order, and test evidence selection by criterion. A criterion chain fails if
a required precursor is absent even when the last event exists.


# Build

Provide one shared event catalog and generated safe-attribute validators for browser, server, worker,
provider, agent, and runtime code. Use OpenTelemetry-compatible interfaces. Product modules emit
named domain facts; export adapters select the telemetry vendor at deployment.


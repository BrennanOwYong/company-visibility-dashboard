# Setup

Local setup writes structured signals to a project-local collector and makes correlation queries
available to tests. It includes a restricted sink for redaction failures and fixtures that contain
known fake secrets. Production exporter endpoints and credentials are external setup.


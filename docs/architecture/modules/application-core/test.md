# Test

Test workspace isolation, transaction rollback, idempotency conflict, lease expiry and reclaim,
duplicate worker delivery, event replay, cursor authorization, migration forward/backward
compatibility, and crash recovery. `bin/project-test` will run these checks before browser journeys.


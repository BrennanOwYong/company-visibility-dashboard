# Logs and feedback

Emit safe request, transaction, job, lease, replay, and migration events through `observability`.
Join jobs to the originating interaction and request. Alert on queue age, exhausted attempts, lease
churn, event replay failure, workspace-denial anomalies, and migration health.


# Iterate

Change use-case interfaces before storage detail. A data-model change must update the data contract,
migration, fixtures, event schema, module tests, and affected feature views. Do not let a feature
bypass workspace checks, transactions, or durable jobs for local convenience.


# Iterate

A new component or transformation needs a schema entry, deterministic implementation, validator
rule, all data-state fixtures, accessibility checks, safe limits, and page-builder examples. A model
change cannot change the output contract. A published schema version remains readable until all
pages have moved through an explicit compatible revision.


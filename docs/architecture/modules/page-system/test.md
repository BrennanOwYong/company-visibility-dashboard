# Test

Test suggestions, clarification, missing connection, unavailable capability, secret input,
confirmation snapshots, worker interruption, bounded repair, invalid agent output, injection, schema
limits, unit mismatch, component accessibility, atomic publish, retry idempotency, return visit,
partial source failure, Memory-version conflict, and prior-version preservation. Browser journeys
must join progress and render events to the confirmed build.


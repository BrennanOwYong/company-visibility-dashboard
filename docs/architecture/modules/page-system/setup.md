# Setup

Local setup uses deterministic Memory, capability, model-output, metric, and failure fixtures. Seed
one valid page for each component and data state. A live model or social provider is not required for
normal validation. Approved external setup supplies live smoke tests and cost controls.


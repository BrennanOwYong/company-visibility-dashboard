# Module: page-system

## Responsibility

Own page conversations, suggestions, request clarification, feasibility, user confirmation, durable
build jobs, constrained agent execution, validation, atomic publication, page-definition versions,
page queries, and source availability states.

## Repository paths

Planned: `packages/page-system/`, `packages/page-definition/`, and trusted renderer components in
`apps/web/`.

## Public interfaces

`../../contracts/page-builder-agent.md`, `../../contracts/page-definition.md`, page routes in
`../../contracts/http-api.md`, and provider capabilities from `provider-integrations`.

## Current feature ownership

Primary owner for `page-builder-chat` and `generated-dashboard-pages`; supplies published navigation
entries to `dashboard-shell`.

## Decisions

See `decisions.md`.

## Current-PRD lessons

See `current-prd-lessons.md`.


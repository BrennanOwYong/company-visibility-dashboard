# Logs and feedback

Emit conversation start, redaction, clarification category, request confirmation, blocker type,
build state transition, model request outcome and usage, validation rule outcome, publication, query
plan outcome, widget state, and return render. Never log request text, summary text, page definition,
metric values, model prompt, or model output.


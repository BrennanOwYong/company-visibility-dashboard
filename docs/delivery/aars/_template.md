# Builder AAR draft — TICKET-ID

The AAR is an advisory optimization record. It does not repeat the technical architecture and it
does not block mandatory testing.

## Repeated module-specific steps

List setup, data preparation, launch, inspection, and test procedures that required repeated
exploration. State the exact reusable sequence.

## Skill candidates

For each repeated sequence, state whether it should become a script, a Codex skill, or remain local
knowledge. Link a candidate when one was created. State `(none found)` when there was no repetition.

## Module trip-ups

Record only non-obvious facts that remain useful under the current PRD. Do not repeat architecture
that is already in `docs/architecture/`.
